[phases.setup]
nixPkgs = ["nodejs", "npm"]
cmds = ["npm --version"]

[phases.install]
cmds = ["npm install --no-audit"]
depsCmd = ["npm install --no-audit"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:railway" 