/**
 * Test de base archivé
 */

import React from 'react';
import { render } from '@testing-library/react';

describe('Archive Tests', () => {
  it('should pass a basic archived test', () => {
    expect(true).toBe(true);
  });
}); 